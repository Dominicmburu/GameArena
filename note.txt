// command to run the vps in my machine
ssh -i C:\Users\Dominic\.ssh\gamingsystem-key.pem ubuntu@13.62.222.49

// shows container running
docker ps 

// shows logs
docker logs gamearena 

# SSH into EC2
ssh -i your-key.pem ubuntu@13.62.222.49

cd ~/gamearena

# Check when images were pulled (should show recent time)
docker images | grep gamearena

# Check container creation time
docker compose ps

# Check logs for your changes
docker compose logs backend --tail=50
docker compose logs frontend --tail=50

# Look for initialization messages that show new code is running

// restart containers


In big screens such as my monitor which is 24 inches, the system looks bad. can you atleast have a max width for big screens.
Then i need us to still work on the hero section.
I want to add multiple images of game. Make transitions/animations look good. Increase the fonts. The images and content for that image should auto scroll the